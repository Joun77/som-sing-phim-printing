แผนการพัฒนาและปรับปรุงระบบสำหรับ AntiGravity Agent(AntiGravity Implementation & Remediation Plan: Som-Sing Phim ERP)1. บริบทและวัตถุประสงค์ (Executive Context & Instructions for AntiGravity)โปรเจกต์: Som-Sing Phim Printing System (ERP/MIS สำหรับธุรกิจโรงพิมพ์)เทคโนโลยีหลัก:Backend: Go (Gin Framework) + PostgreSQL (Database Migration Scripts)Admin Frontend: React (Vite) + Tailwind CSS + Zustand / TanStack QueryCustomer Frontend: React (Vite) + Tailwind CSSคำสั่งสำหรับ AntiGravity Agent:ให้ปฏิบัติตามลำดับงาน (Task Execution Sequence) ในเอกสารฉบับนี้อย่างเคร่งครัด โดยเน้นย้ำเรื่องความสะดวกในการใช้งานของผู้บริหารคนเดียว (Single Owner Full Access) ความถูกต้องทางการเงิน (Decimal Precision) ความเสถียรของคลังสินค้า (Transaction Locking) และโมดูลการเงินสำหรับผู้บริหาร2. ลำดับงานพัฒนาและแก้ไขระบบ (Development Task Breakdown)[ Sprint 1: P0 Owner Auth & Financial Precision ] ──> [ Sprint 2: P1 Finance & Core ERP ] ──> [ Sprint 3: P2 Advanced Features ]
- Owner Single-Login UI & Token Persistence          - Owner Finance & Accounting Module     - Internal Shipping Tracking
- Full-Access Admin Middleware (Go)                  - Unified State (Zustand + TanStack Query)- Offcut Cost Rebate Engine
- Decimal Precision (Go/SQL)                         - Dynamic Deposit & Tax Option          - SSE Realtime Progress Stream
- Inventory FIFO Row Lock (FOR UPDATE)               - DB Audit Logging Hooks
Phase 1: ความสะดวกของผู้บริหาร ระบบล็อกอิน และความถูกต้องทางการเงิน (P0 - Immediate Fixes)Task 1.1: ระบบยืนยันตัวตนผู้บริหารคนเดียว (Single Owner Auth & Admin Access)เป้าหมาย: สร้างหน้า Login ที่สะดวกรวดเร็วสำหรับผู้บริหารคนเดียว เข้าถึงได้ทุกโมดูลโดยไม่มีการจำกัดสิทธิ์งานที่ต้องทำ:Frontend (admin-system/frontend/src):สร้างหน้า LoginPage.tsx ดีไซน์เรียบง่าย รองรับการกด Remember Me (Persistent Login Session)สร้าง useAuthStore.ts (Zustand) เก็บ Auth Token และข้อมูล Owner Accountสร้าง ProtectedRoute.tsx ตรวจสอบสถานะการล็อกอิน หากล็อกอินแล้วให้เข้าถึงได้ทุกหน้า (/orders, /finance, /inventory, /equipment, /hr)Backend (admin-system/backend/auth):พัฒนา Handler POST /api/v1/auth/login สำหรับ Owner Account ออก JWT Token สิทธิ์ Super-Adminสร้าง Middleware RequireAuth() ใน auth/middleware.go ป้องกัน API แต่ให้อำนาจ Owner เข้าถึงได้ทุก EndpointsTask 1.2: แปลงการคำนวณราคาทั้งหมดเป็น Decimal (Financial Precision Fix)เป้าหมาย: ขจัดปัญหา Rounding Errors จาก Floating-point (float64) ทั้งหมดในระบบงานที่ต้องทำ:Go Backend: นำไลบรารี github.com/shopspring/decimal มาใช้ใน backend/pricing/engine.goPostgreSQL Schema: สร้าง Migration SQL 006_convert_floats_to_decimal.sql แปลงคอลัมน์ราคาและอัตราแลกเปลี่ยนเป็น DECIMAL(18,4)Task 1.3: ป้องกัน Race Condition ในการตัดสต็อก FIFO (Inventory Concurrency Control)เป้าหมาย: ป้องกันการตัดสต็อกติดลบหรือตัดสต็อกซ้ำซ้อนเมื่อมี Order เข้าพร้อมกันงานที่ต้องทำ: ใช้ Transaction SQL ใน Go พร้อม SELECT ... FOR UPDATE ใน backend/inventory/assets.goPhase 2: โมดูลการเงิน บัญชี และโครงสร้างหลัก ERP (P1 - Core ERP & Finance Page)Task 2.1: พัฒนาหน้าจัดการการเงินและบัญชีสำหรับผู้บริหาร (Owner Finance Module)เป้าหมาย: สร้างหน้าการเงินแบบครบวงจรให้ผู้บริหารดูภาพรวม ตรวจสลิป และออกใบเสร็จ/ใบกำกับภาษีได้ในจุดเดียวงานที่ต้องทำ:Frontend (admin-system/frontend/src/features/finance):สร้าง FinanceDashboard.tsx: KPI ยอดขายรวม, ยอดลูกหนี้ AR ค้างชำระ, ยอดรออนุมัติสลิป, และอัตรากำไรขั้นต้น (พร้อมปุ่มสลับสกุลเงิน LAK / THB / USD)สร้าง PaymentVerificationTable.tsx: ตรวจดูสลิปโอนเงิน พร้อมปุ่ม Approve / Reject อนุมัติยอดทันทีสร้าง InvoiceTaxDocumentModal.tsx: ออกใบแจ้งหนี้, ใบเสร็จมัดจำ, ใบเสร็จรับเงิน, และใบกำกับภาษี (รองรับ Option ภาษี: No Tax / VAT Excluded / VAT Included)สร้าง JobProfitabilityAudit.tsx: เปรียบเทียบราคาประเมิน vs ต้นทุนการผลิตจริง (กระดาษ FIFO + หมึก + ค่าแรง + ค่าเสื่อม)Backend (admin-system/backend/finance):พัฒนา Endpoints: GET /api/v1/finance/summary, POST /api/v1/finance/verify-slip, GET /api/v1/finance/ar-agingTask 2.2: ระบบมัดจำยืดหยุ่นและตัวเลือกภาษี (Dynamic Deposit & Tax Toggle)เป้าหมาย: รองรับการกำหนดมัดจำแบบยืดหยุ่น ($0\%, 30\%, 50\%, 100\%$) และระบบสลับคิด/ไม่คิด VATงานที่ต้องทำ:อัปเดต Schema ตาราง quotations และ orders เพิ่มคอลัมน์ deposit_percentage, tax_mode (NONE, EXCLUDED, INCLUDED), และ tax_rateปรับปรุง pricing/engine.go ให้คำนวณยอดภาษีและยอดมัดจำตาม Option ที่เลือกTask 2.3: ยุบและจัดระเบียบ State Management (Unified Frontend State)เป้าหมาย: ย้าย State ทั้งหมดไปใช้ Zustand ร่วมกับ TanStack Query และลบ Mock Data ออกจาก Production ComponentsPhase 3: ฟีเจอร์ระดับสูงและการติดตามพัสดุ (P2 - Feature Enhancements)Task 3.1: ระบบติดตามพัสดุภายในโรงพิมพ์ (Internal Shipping Tracking System)เป้าหมาย: บันทึกและแสดงผลการจัดส่งโดยใช้ระบบ Internal Tracking ของโรงพิมพ์เองงานที่ต้องทำ:เพิ่มฟิลด์ internal_tracking_code, courier_name, และ pod_image_url ในตาราง ordersแสดงข้อมูลการจัดส่งบนหน้า TrackingPage.tsx ฝั่ง Customer PortalTask 3.2: ระบบคิดส่วนลดเศษกระดาษตัดเหลือ (Offcut Cost Rebate Engine)เป้าหมาย: นำเศษกระดาษที่เหลือจากการตัด (Offcuts) กลับมาคำนวณเป็นส่วนลดต้นทุนในใบเสนอราคาTask 3.3: ระบบแจ้งเตือนสถานะงานพิมพ์แบบ Realtime (SSE Notifications)เป้าหมาย: ส่งข้อมูลสถานะการผลิตจาก Kanban Board ไปยังหน้า Customer Portal ด้วย Server-Sent Events (SSE)3. เกณฑ์การตรวจสอบความถูกต้อง (Definition of Done & Verification)Owner Access Test: ผู้บริหารล็อกอินแล้วสามารถเข้าถึงและจัดการได้ทุกหน้า (/orders, /finance, /inventory, /equipment, /hr) โดยไม่มีข้อผิดพลาดเรื่องสิทธิ์เข้าถึงFinance & Slip Audit Test: เมื่อกด Approve Payment ในหน้า Finance สถานะออเดอร์ต้องเปลี่ยนเป็น Paid และปลดล็อกให้งานวิ่งเข้าสู่ Production Board ทันทีCompilation & Precision Check: go build ./... และ npm run build ต้องผ่านโดยไม่มี Error และการคำนวณภาษี/มัดจำต้องเที่ยงตรงตามหลัก Decimal