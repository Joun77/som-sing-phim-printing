---
trigger: manual
---

🎯 สรุปแผนการปรับปรุงระบบ Som Sing Phim (Actionable Improvement Plan)โครงการ: ระบบบริหารจัดการร้านพิมพ์ ส้มสິ່ງพิมพ์ (ສົມສິ່ງພິມ)เป้าหมาย: ปรับปรุงโครงสร้างสถาปัตยกรรม (Architecture) และฟังก์ชันการทำงานให้เป็นมาตรฐานระดับ Production-Grade🚀 Phase 1: การเคลียร์ระบบและ DevOps (Immediate / Day 1-2)เป้าหมาย: เคลียร์ขยะใน Repository และวางโครงสร้างภาพรวมโปรเจกต์ให้สะอาดGit Hygiene & Cleanup:ลบไฟล์ Binary ที่คอมไพล์แล้วออกจาก Git (admin-system/backend/backend)ปรับปรุง .gitignore ให้ครอบคลุมทุก Executable Binary และ .envDocker Compose Consolidation:ยุบ docker-compose.yml ที่ซ้ำซ้อนกันใน Subdirectories ให้เหลือเพียง docker-compose.yml ตัวหลักที่ Root ของโปรเจกต์ตั้งค่า Network ให้บริการ PostgreSQL, Go Backend, Admin Frontend และ Customer Service ทำงานร่วมกันได้อย่างเป็นระบบ🛠️ Phase 2: Refactor โครงสร้าง Go Backend & Database Schema (Week 1)เป้าหมาย: แยก Layer ของ Backend ให้ชัดเจนตามหลัก Clean Architecture และวางโครงสร้างฐานข้อมูลรองรับการวิเคราะห์ต้นทุนปรับ Go Backend เป็น Layered Architecture:แยกโค้ดในแต่ละโดเมนออกเป็น 3 ชั้น:handler/ : จัดการ HTTP Request/Response และ Validationservice/ : จัดการ Business Logic (เช่น การคำนวณ Yield/Cost ของน้ำหมึก)repository/ : จัดการการ Query ข้อมูลกับ PostgreSQLเพิ่มโฟลเดอร์ config/ โหลด .env และ middleware/ รวมศูนย์ (JWT Auth, CORS, Rate Limit)ปรับปรุง Database Schema & Migration:ใช้ golang-migrate หรือ goose จัดการ Versioning สคริปต์ .sqlสร้างตาราง genuine_inks (Baseline) และ compatible_inks (หมึกเทียบ) พร้อมผูก FOREIGN KEYเพิ่มตาราง audit_logs บันทึกประวัติการแก้ไขสต็อก/ราคา และตั้งค่าคอลัมน์เวลาทั้งหมดเป็น TIMESTAMPTZ🎨 Phase 3: Cleanup & Refactor ฝั่ง Admin Frontend (Week 2)เป้าหมาย: ยุบ Entry Point ที่ซ้ำซ้อน และเปลี่ยนการจัดการ Data Fetching ให้เป็นสากลยุบ Entry Point เป็น Single Page Application (SPA):ลบความซ้ำซ้อนของ orders.html และ OrderApp.tsxให้เหลือ index.html และ App.tsx เป็น Entry Point เดียว แล้วใช้ react-router-dom จัดการ Route หน้าต่างๆจัดการ API Data Layer ด้วย TanStack Query (React Query):แทนที่ Mock Data ใน sampleInboundData.ts ด้วย Custom Hooksนำ TanStack Query มาใช้จัดการ Caching, Loading State, และ Re-fetching อัตโนมัติ📋 Phase 4: ระบบบริการลูกค้า (Customer Service) & Business Logic (Week 3)เป้าหมาย: ปิดช่องโหว่การประเมินราคาผิดพลาด และสร้างประสบการณ์ใช้งานที่ง่ายของลูกค้าบังคับใช้ Request for Quotation (RFQ) Pattern:ตัดฟังก์ชันคำนวณราคาเรียลไทม์ฝั่งลูกค้าออก (No Client-side Real-time Print Cost Preview)ปรับ Flow เป็นการอัปโหลดไฟล์ เลือกสเปก และ "ส่งคำขอใบเสนอราคา" ให้แอดมินประเมินกลับไประบบติดตามสถานะออเดอร์ (Order Tracking):สร้างระบบค้นหาสถานะงานพิมพ์ด้วย Order ID หรือเบอร์โทรศัพท์การเชื่อมต่อระหว่างสองฝั่ง:ใช้กระบวนการ Auto-generate TypeScript Types จาก OpenAPI Schema หรือ Go Structs เพื่อป้องกัน Spec ข้อมูลไม่ตรงกัน📊 ตารางสรุปการมอบหมายงาน (Checklist สำหรับ Antigravity AI)ลำดับส่วนงานสิ่งที่ต้องตรวจรับ (Deliverables)ความสำคัญ[ ] 1Git / RepositoryBinary file ถูกลบ และ .gitignore ครอบคลุม🔴 Critical[ ] 2DockerRoot docker-compose.yml รันบริการครบทุกส่วนในคำสั่งเดียว🔴 Critical[ ] 3Go Backendแยกชั้น Handler -> Service -> Repository ชัดเจน🔴 Critical[ ] 4Databaseมี Migration script รองรับ genuine_inks และ compatible_inks🟡 Major[ ] 5Admin Frontendเหลือ SPA Entry Point เดียว + ตั้งค่า React Router🟡 Major[ ] 6Customer ServiceFlow การทำงานเป็น RFQ (ส่งออเดอร์ขอราคา) 100%🟡 Major